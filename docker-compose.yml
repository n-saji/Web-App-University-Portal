version: '3.9'

services:
  college_adminstration:
    container_name: collegeadminstration
    build:
      context: ./
    image: collegeadminstration
    # depends_on:
    #   - localhost
    environment:
      DB_HOST: college.c5rvl144axxo.ap-south-1.rds.amazonaws.com
      DB_PORT: 5432
      DB_USER: postgres
      DB_PASSWORD: paul_202k
      DB_NAME: random

    ports:
      - 5050:5050
  # localhost:
  #   image: postgres
  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: paul_202k
  #   ports:
  #     - 5432:5432
#     volumes:
#       - pg_db:/var/lib/postgresql/data
# volumes:
#   pg_db:
#     driver: local